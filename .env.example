# Copy this to `.env` and set values locally for development tests
# Supabase
SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_SERVICE_KEY=<service-role-key>
SUPABASE_ANON_KEY=<anon-key-if-needed>

# OpenAI
OPENAI_API_KEY=sk-<your-openai-key>

# Worker / Wrangler (optional) - for local dev or wrangler secrets
# You can set these via `npx wrangler secret put ENVNAME`
# Example: npx wrangler secret put OPENAI_API_KEY

# Other runtime settings
NODE_ENV=development

# Example: local override for testing
# SUPABASE_URL=http://localhost:54321
# SUPABASE_SERVICE_KEY=local-service-role-key
